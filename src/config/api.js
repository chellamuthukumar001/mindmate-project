// API Configuration
// Uses relative URLs so both Vite proxy (dev) and Vercel rewrites (production) work correctly.
export const API_ENDPOINTS = {
    chat: '/chat',
    health: '/health',
    moods: '/api/moods',
    journals: '/api/journals',
};
